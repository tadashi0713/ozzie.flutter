name: ozzie
description: Ozzie is your testing friend. Ozzie will take an screenshot during integration tests whenever you need. Ozzie will capture performance reports for you.
version: 2.2.3
author: Jorge Coca<jcocaramos@gmail.com>
homepage: https://github.com/jorgecoca/ozzie.flutter

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  archive: ^3.1.2
  meta: ^1.3.0
  test: ^1.16.8
  yaml: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.10

flutter:
